define(['jquery', 'oae.core'], function($, oae) {

    return function(uid, showSettings, widgetData) {

        // The widget container
        var $rootel = $('#' + uid);

        var initToDo = function() {
		$(document).on('click', '.oae-trigger-createmeeting', function(ev) {
			console.log("hola");
			$('#createmeeting-modal').modal('show')
		});	
        };
	
	initToDo();
    	};
	
	
})